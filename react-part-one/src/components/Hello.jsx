export default function Hello() {
    return(

        //This can only return one parent element
        //In this case, <div> is the parent element while <p> is a child element 
        
       <div>
          <p>Hello World!</p>
       </div>
    );
 }